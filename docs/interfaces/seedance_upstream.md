# Seedance / 火山方舟原生视频任务

本文覆盖 Ark 异步视频任务的账号准入、原生协议、任务归属和完成后计费。Seedance 复用 OpenAI API Key 账号和媒体任务基础设施，不新增平台枚举，也不改变 Grok 视频的按秒计费规则。

<a id="seedance_task_lifecycle"></a>
## 账号与任务生命周期

账号必须属于 OpenAI、类型为 API Key、配置自定义 Base URL，并显式选择 `openai_workload_capabilities` 中的 `seedance`。管理端创建、编辑及批量修改均提供该选项；未配置能力的旧账号继续默认文本与 Embeddings，不自动获得视频能力。方舟 Base URL 可设置为 `https://ark.cn-beijing.volces.com/api/v3`，站点根地址会补齐 `/api/v3`，代理地址已有路径前缀会保留。模型 ID 可以是方舟模型名或模型推理接入点 ID，仍受分组、渠道及账号模型规则限制。

内部 Redis 调度精简投影必须同时保留 `base_url` 和显式能力字段，确保缓存候选与完整账号的资格判断一致；地址缺失时不能仅凭能力开关放行。升级前缓存若缺少地址，由现有启动全量重建或账号更新 outbox 重建恢复，无需清空 Redis。启动重建在后台执行，完成前旧缓存可能暂时拒绝视频请求；滚动升级需完成所有节点更新，避免旧进程再次写入缺字段的投影。OAuth、未勾选能力和未配置地址的账号仍不可用于 Seedance。

支持以下等价任务根路径：

- `/api/v3/contents/generations/tasks`
- `/v3/contents/generations/tasks`
- `/v1/contents/generations/tasks`
- `/contents/generations/tasks`

`POST` 根路径创建任务，`GET /:task_id` 查询，`DELETE /:task_id` 删除。创建 JSON 必须包含非空 `model` 和非空 `content` 数组；扩展多模态字段原样转发，不转换为 Responses 或 Chat Completions。模型映射只改写请求侧 `model`，原生响应不进行客户端模型别名还原，不包装成平台业务响应。上游 HTTP 错误保留其状态和正文，并进入现有运维错误采集链路。

创建复用现有鉴权、计费准入、并发槽及媒体生成权限；当前分组的媒体入口沿用“允许图片生成”开关。智能路由可在提交前选择具备模型和视频能力的 OpenAI 分组，任务发出后不自动换账号或跨分组重放，避免含糊失败造成重复创建付费视频。

创建前检查任务缓存可访问，上游创建成功后先持久化用户、API Key、分组和上游账号归属及结算快照，再向客户端提交原生响应；保存使用独立的有限时上下文，避免客户端断连立即取消缓存写入。缓存前置检查不是分布式事务；受理后归属或快照最终仍无法写入时，返回 `503`、`TaskStorageUnavailable` 和已创建的原始任务 ID，提示不可重新提交，运维可凭任务 ID 排查，服务端不自动重创或取消付费任务。

内部任务键增加 `seedance:` 前缀以隔离 Grok 同名任务，客户端收到的 ID 仍为上游原值。查询和删除必须匹配创建者与 Key，并固定原账号；不会拿另一个账号尝试相同任务 ID。普通 Key 改绑分组后不自动取得旧分组任务的查询权限；复合/智能 Key 查询可按已保存归属恢复原分组，并继续校验可访问范围。

查询、删除不是新的消费请求，因此可在余额或 Key 额度用尽后继续读取、取消已有任务；用户/Key 禁用、IP 限制、租户权限和任务归属检查仍然有效。仅精确匹配的任务详情路径享受此边界，集合查询、未知路径及嵌套路径不能借此绕过消费准入。

<a id="seedance_billing"></a>
## 完成后计费与恢复边界

任务创建时保存计费模式、选定订阅、分组配置、原始客户端模型与渠道映射快照，剔除预加载账号及用户资料。查询时使用创建快照恢复扣费上下文；改动 Key 的结算方式或另购套餐不会把旧任务转移到新套餐。自动模式通过仅服务端设置的 `SubscriptionScopeID` 限制原套餐参与分摊，余额继续按现有规则补足；创建时无套餐则固定走余额。该约束不改变普通请求候选订阅和幂等指纹。

快照固定的是结算选择，不是额度预占。完成时原套餐到期、失效或不再覆盖分组，自动模式可由余额承担；指定订阅模式仍按现有规则报告结算失败，不擅自改扣另一套餐。指定订阅仍有效但额度不足的已放行任务，保留原有溢出余额欠费策略。尚无后台补偿任务，管理员应处理持续结算失败日志，不能因为客户端已拿到视频就认定扣费成功。

只有查询返回 `status=succeeded` 且 `usage.completion_tokens` 为正数才进入结算，复用现有输出 token 价格、分组倍率、余额/订阅扣费和使用记录链路。管理员必须为实际结算模型配置输出 token 单价。缺少或为零的 usage、未完成和失败任务均不会按时长推算费用；此路径不使用 Grok 视频按秒价。

结算使用任务级 Redis 领取和稳定的计费请求 ID，重复、并发轮询不会生成多笔相同结算。已观察到成功 usage 后，即使客户端读完响应立即断连，仍使用有界独立上下文准备结算；用量队列满时走强制执行路径，不丢弃扣费任务。扣费失败会释放领取供后续查询重试；快照缺失或归属不符时记录异常，不猜测套餐或计费模型。

任务归属和待结算快照使用现有 Redis 24 小时存活期；需保留 Redis 数据，不能只备份 PostgreSQL。当前没有后台自动轮询，客户端须在有效期内查询完成结果以触发结算。计费领取沿用 48 小时缓存标记：正常错误会释放，但进程在领取后、实际结算前硬退出时无法执行释放，也没有持久化 outbox 自动恢复，需运维核对任务及账单。该限制与异步接口的结算时点有关，不能把创建响应视为已结算凭证。

## 相关文档

- [OpenAI 上游](openai_upstream.md)
- [路由与结算](../domains/routing_and_billing.md)
- [智能路由 API Key](../domains/smart_routing_api_keys.md)
- [部署与数据库迁移](../operations/deployment_and_migrations.md)
