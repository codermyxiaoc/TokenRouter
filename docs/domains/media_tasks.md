# 异步图片与任务记录

本文描述第三方 API Key 异步图片接口与用户、管理员共用的任务记录。页面统一显示为“任务记录”，路由与内部 `media_tasks` 标识保持兼容。图片任务恢复自 [Wei-Shaw/sub2api 的异步图片实现](https://github.com/Wei-Shaw/sub2api/blob/b0e32fe5be4017e21a89f383fa5952444bca9d15/docs/ASYNC_IMAGE_TASKS.md)，执行和计费适配当前 fork 的完整网关链路。创作台与 Gemini/Vertex 批量图片继续使用各自的任务体系和页面，详见[创作台](creative_studio.md)、[批量图片作业](batch_image_jobs.md)。

## 章节导航

- [异步图片生命周期](#async_image_lifecycle)：提交、后台执行、结果保存、查询与失败边界。
- [任务记录观测](#media_task_observation)：列表来源、权限、状态与费用关联。
- [配置与上线](#配置与上线)：对象存储、迁移、多实例与保留期限。

<a id="async_image_lifecycle"></a>
## 异步图片生命周期

`POST /v1/images/generations/async`、`POST /v1/images/edits/async` 接受与同步 Images 相同的 JSON/multipart 请求，兼容无 `/v1` 别名，不接受流式请求。异步入口提前拒绝缺失、非字符串或全空白 `prompt`，返回 400 且不受理任务；不修改同步接口的参数规则。功能默认关闭，只有 `image_storage` 已开启、S3 必要配置完整且初始任务可落入 PostgreSQL 时才接受任务。提交仍要求有效 Key、图片权限和 OpenAI/Grok 分组；正常提交返回 HTTP 202、任务 ID、相对 `poll_url`、`Location` 和 `Retry-After: 3`。客户端必须主动改用这两个端点并轮询，原有同步 SDK 请求不会自动转成任务。

提交前在模型改写之前惰性捕获原始请求。后台在同一进程内部重新进入同步 Images 完整路由，保留可信内部请求 ID，并重新执行认证、模型映射、复合 Key/智能路由选组、并发限制、审核与现有结算；不能直接调用单个 Images handler 绕过外层网关策略，也不能把已经映射过的模型再次送入映射。图片沿用现有安全边界，不加入文本请求的智能跨组失败重放；客户端提交连接结束不取消已接受任务。

完成后的图片先转存 S3，`b64_json` 改写为 `data[].url`。PostgreSQL `image_tasks` 保存任务、执行租约和紧凑结果，Redis 是镜像与临时补偿存储；原有 usage 等 JSON 元数据仍保留。两处都不保存请求正文、API Key 或图片 Base64。URL 下载验证公共目标、重定向与实际拨号地址，不携带上游凭据，不使用环境代理；每张图片默认最多 32 MiB，仅接受 PNG/JPEG/WebP/GIF，转存后的结果 JSON 限制为 1 MiB。缺少图片或空 `data` 不能标为完成。提交时固定存储快照，因此处理中修改/关闭图片存储不会将大图片写回任务数据库或 Redis。

图片下载与上传对网络临时错误、HTTP 429/5xx 分别最多调用三次，退避 250/500 毫秒，并共享存储阶段截止时间；底层 S3 SDK 自身的重试也受该截止时间约束。上传重试复用本张已下载字节和稳定对象键，不重跑此前成功图片、不调用模型、不新增扣费。权限、参数、图片内容、安全校验错误不重试。平台不缩放上游图片，响应实际尺寸由上游决定。

`GET /v1/images/tasks/:task_id`（及无前缀别名）要求原用户和原 Key，跨用户或跨 Key 与不存在任务统一返回 404。查询不调用生成、不结算，已接受任务在功能关闭后仍可查询；余额不足或额度耗尽不应阻止取回结果，但身份与 Key 有效性检查保留。任务状态为 `processing`、`completed`、`failed`，失败时在 HTTP 200 的任务对象中返回生成阶段的 `http_status` 与 `error`，不是再次生成。

图片仍由同步网关的既有价格与用量规则结算，包括渠道 Token 价格、图片价格、订阅/余额、倍率及幂等扣费。异步模块和列表都不增加第二次结算。生成成功而对象存储失败时，任务会失败但生成用量可能已经扣费；不能因存图失败自动重新生成或退款。

后台生成拥有独立的 30 分钟超时，图片转存另有 2 分钟预算；仅内部异步上下文让上游脱离客户端取消时保留这一截止时间，普通同步请求语义不变。每实例最多受理 64 个尚未结束的异步任务（含排队、执行及存图），占满时新提交返回 503，不能通过快速获取 202 无限堆积原图与生成结果。现有用户/账号并发及排队限制仍在后台执行链生效；这是本机受理保护，不是跨实例持久队列。

提交及每次终态写入分别设置 24 小时结果保留期限，响应 `expires_at` 是当前记录的过期时间。PostgreSQL 中任务结果与 `media_tasks` 投影同事务提交，Redis 写失败不影响已保存结果；查询可直接从 PostgreSQL 返回。数据库终态写入短暂失败时，仅允许已受理且执行身份匹配的任务先保存在 Redis，由后台维护或合法查询补写数据库和列表。终态入库使用独立预算及有界重试，上传耗尽自身预算不应阻止保存终态。

每个新任务有 90 秒执行租约，执行实例每 15 秒续租；硬截止时间为生成预算加 3 分钟，覆盖上传和收尾。后台每 15 秒限量检查遗留任务，失联或超过硬期限的任务记为 `failed / 503`，错误类型为 `execution_interrupted`，明确告知结果不确定且可能已计费。行锁内复核租约，多实例不能凭启动事件误判其他活跃实例；同一执行者稍后保存的真实结果可纠正维护器的中断判定。已完成结果不因普通重启或 Redis 数据丢失而丢失，过期结果记录定量清理，列表历史保留。

执行仍是进程内同步上游调用，不是可以安全重放的持久队列；未收到上游结果就退出时不能保证恢复图片。恢复程序不调用生成、不结算、不自动退款。升级前的 Redis 任务可以兼容读取并回填，旧任务没有心跳时使用创建后 32 分钟执行预算，不立即中止旧实例任务。PostgreSQL 与 Redis 同时持续不可写、或者进程在图片上传/结果持久化之前退出，仍可能无法找回结果；此时应核对错误及用量记录，不可承诺“所有故障下都续跑”。

<a id="media_task_observation"></a>
## 任务记录观测

`media_tasks` 是独立的 PostgreSQL 元数据投影，不持久化请求正文、图片 Base64、生成文件或视频下载地址；短期图片结果另存 `image_tasks`，不暴露在通用列表中。来源为：

| 来源 | 类型 | 状态更新入口 |
| --- | --- | --- |
| `async_image` | 图片 | 上述异步图片提交及后台结束 |
| `grok_video` | 视频 | 现有 Grok 视频创建/查询/编辑/扩展流程 |
| `seedance_video` | 视频 | 现有 Seedance 原生视频创建/查询/删除流程 |

以 `(source, task_id, api_key_id)` 唯一约束去重，保存创建、更新、完成及过期时间。并发更新使用单条 UPSERT，已经观测到的完成、失败、取消和过期终态不被迟到的处理中状态覆盖。异步图片以受行锁保护的 `image_tasks` 状态在同一事务内更新投影，允许同执行真实终态纠正 `execution_interrupted`，不允许迟到处理状态回退。未完成且超过 `expires_at` 的记录在查询时显示过期。视频供应商状态归一化为 `queued`、`processing`、`completed`、`failed`、`cancelled`、`expired`；HTTP 查询失败本身不能被当作生成失败。失败摘要使用固定分类文本，原始上游错误仍由现有错误日志或短期图片查询保存。

用户页面 `/media-tasks` 通过 `GET /api/v1/media-tasks` 和 `GET /api/v1/media-tasks/:id` 只能读取自己的任务，即使传入其它用户 ID 也不能越权，且不暴露上游账号 ID。管理员页面 `/admin/media-tasks` 使用同名 `/api/v1/admin` 接口读取全部任务，支持用户 ID 筛选。两种列表都支持媒体类型、状态、来源、模型、分页；个人入口中的管理员也只看自己。接口沿用面板 JWT/管理员认证与分页 envelope，返回 `Cache-Control: no-store`。

管理员列表与详情额外返回安全的 `user` 身份摘要（`id`、`email`、`username`、可选 `deleted_at`），查询时关联当前用户资料，不复制完整用户对象或增加任务写入字段。页面沿用使用日志的名称优先、邮箱缩略回退与 `#ID` 展示，悬停可查看完整名称和邮箱；软删除用户显示删除标记，关联缺失时仍保留任务及用户 ID。个人接口不返回该摘要，页面也不显示管理员用户列。

列表是只读观测面板，刷新不会查询上游、触发扣费、自动恢复或重新生成。视频需要原客户端继续调用原来的状态/内容查询接口才更新状态；Seedance/Grok 原有任务归属和完成结算流程不变。旧历史任务不会自动回填，旧视频在后续被合法查询时可进入列表。完整视频契约见 [Grok](../interfaces/grok_upstream.md)、[Seedance](../interfaces/seedance_upstream.md)。

`actual_cost` 和 `billing_mode` 按同用户、同 Key、同请求 ID 从使用记录关联。图片使用普通网关的 `client:<内部 ID>`，视频使用原有稳定任务结算 ID。费用为空表示尚未找到使用记录，不等于免费，也不能仅凭任务完成推断已扣费；明确零费用保留为零。智能路由最终分组、平台与管理员可见的账号优先取实际使用记录，模型保持提交时记录的名称。费用写入存在异步延迟，最终以既有使用记录与账务为准。

## 配置与上线

- 数据库使用 `282_media_tasks.sql` 建立列表投影，新增 `283_image_tasks_durable_results.sql` 建立短期结果与租约，不修改已执行迁移，不改余额、订阅或渠道定价表。新应用正常启动时自动迁移。
- 管理后台“数据备份”页面新增“异步图片存储”。管理员可独立配置 S3，也可复用备份 S3 的连接和凭据，图片单独设置 Bucket/Prefix。配置保存在运行时设置中，其中 Secret 加密存储；查询脱敏，空 Secret 保留已有值，保存和测试沿用 step-up。没有后台覆盖时使用 YAML/环境变量中的 `image_storage`；后台保存后本机立即失效缓存，多实例最多约 30 秒更新。
- 存储连通性测试执行 `HeadBucket`；它不等价于完整生图测试，也不保证 PutObject 权限或公开域名下载配置正确。上线前应使用目标存储验证实际上传及访问。
- 未配置公开访问地址时使用有期限的签名链接；配置公开地址时，持有对象地址的人可以访问相应图片。管理员应按数据保留要求设置存储桶权限和生命周期。
- PostgreSQL 结果保留期、Redis 镜像 TTL、签名链接有效期、S3 对象生命周期和任务列表历史保留互相独立；短期结果表由维护循环清理，当前列表元数据没有自动清理任务，S3 文件由存储桶生命周期清理。短期结果含图片访问链接，会进入数据库备份，按业务数据权限保护备份。
- 现有同步 Images、创作台、Gemini 批量图片接口不因异步图片开关而改变。长时间请求仍受上游/代理自身超时影响，异步接口解决的是客户端等待同一条连接的问题。

主要代码入口：`backend/internal/handler/image_task_handler.go`、`service/image_task.go`、`service/image_storage_settings.go`、`repository/image_storage_s3.go`、`service/media_task.go`、`repository/media_task_repo.go`、`handler/grok_media.go`、`frontend/src/views/MediaTasksView.vue`。
